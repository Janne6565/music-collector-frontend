/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Rekordo API
 * Auth, per-entity sync and the MusicBrainz metadata proxy.

The collection itself is not served from here: clients are local-first
and read their library from a local store, so there are no CRUD endpoints
for copies. The server participates only as a sync peer.
 * OpenAPI spec version: v1
 */
import type {
  AvatarDto,
  Upload1Body,
  Upload1Params
} from '../rekordoAPI.schemas';

import { customInstance } from '../../axios-instance';



  /**
 * The original picture plus the square the user framed, in the picture's own pixels. The server renders it, so the circle is identical on every device. Replaces whatever was there; there is no gallery and no history.
 * @summary Set the picture on this account
 */
export const upload1 = (
    upload1Body: Upload1Body,
    params: Upload1Params,
 ) => {const formData = new FormData();
formData.append(`file`, upload1Body.file)

      return customInstance<AvatarDto>(
      {url: `/api/v1/avatar`, method: 'POST',
      headers: {'Content-Type': 'multipart/form-data', },
       data: formData,
        params
    },
      );
    }
  /**
 * Idempotent: an account that has no picture answers 204 as well, since there is nothing to report and nothing went wrong.
 * @summary Go back to the initials circle
 */
export const removePicture = (
    
 ) => {
      return customInstance<void>(
      {url: `/api/v1/avatar`, method: 'DELETE'
    },
      );
    }
  /**
 * Open, and answered the same for everybody. Cached hard: the URL carries the moment the picture landed, so replacing one produces a different URL.
 * @summary One collector's picture
 */
export const content1 = (
    userId: string,
 ) => {
      return customInstance<Blob>(
      {url: `/api/v1/avatar/${userId}`, method: 'GET',
        responseType: 'blob'
    },
      );
    }
  export type Upload1Result = NonNullable<Awaited<ReturnType<typeof upload1>>>
export type RemovePictureResult = NonNullable<Awaited<ReturnType<typeof removePicture>>>
export type Content1Result = NonNullable<Awaited<ReturnType<typeof content1>>>
