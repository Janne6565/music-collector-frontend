/**
 * Generated by orval v7.21.0 🍺
 * Do not edit manually.
 * Rekordo API
 * Auth, per-entity sync and the MusicBrainz metadata proxy.

The collection itself is not served from here: clients are local-first
and read their library from a local store, so there are no CRUD endpoints
for copies. The server participates only as a sync peer.
 * OpenAPI spec version: v1
 */
import type {
  PhotoUploadDto,
  UploadBody,
  UploadParams
} from '../rekordoAPI.schemas';

import { customInstance } from '../../axios-instance';



  /**
 * A photo pictures either a copy or a wishlist entry, so exactly one of `copyId` and `wishId` is given. Naming both, or neither, is a 400.
 * @summary Upload the bytes for a photo the client has already created
 */
export const upload = (
    uploadBody: UploadBody,
    params: UploadParams,
 ) => {const formData = new FormData();
formData.append(`file`, uploadBody.file)

      return customInstance<PhotoUploadDto>(
      {url: `/api/v1/photos`, method: 'POST',
      headers: {'Content-Type': 'multipart/form-data', },
       data: formData,
        params
    },
      );
    }
  /**
 * Clients download once and keep a local copy, so photos work offline.
 * @summary The photo's bytes
 */
export const content = (
    id: string,
 ) => {
      return customInstance<Blob>(
      {url: `/api/v1/photos/${id}/content`, method: 'GET',
        responseType: 'blob'
    },
      );
    }
  export type UploadResult = NonNullable<Awaited<ReturnType<typeof upload>>>
export type ContentResult = NonNullable<Awaited<ReturnType<typeof content>>>
